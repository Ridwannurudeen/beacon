{
  "name": "beacon",
  "version": "0.1.0",
  "private": true,
  "description": "The signal layer of Onchain OS — composable live-priced intelligence for agents on X Layer",
  "workspaces": [
    "packages/*",
    "signals/*",
    "contracts",
    "app"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
