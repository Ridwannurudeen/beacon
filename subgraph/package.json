{
  "name": "@beacon/subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "The Graph subgraph indexing SignalRegistry, AgentRegistry, AtlasVaultV2, CascadeLedger",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ beacon",
    "create-local": "graph create --node http://localhost:8020/ beacon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 beacon"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.80.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
